# 모든 키는 NEXT_PUBLIC_* (브라우저 노출 가능).
# 비밀값은 없다(백엔드 인증 미사용).
# .env.local 파일을 만들고 아래 키를 복사해 환경별 값으로 덮어쓴다.

# 백엔드 REST API 베이스 URL
NEXT_PUBLIC_API_BASE=http://localhost:8080

# 백엔드 WebSocket 베이스 URL
NEXT_PUBLIC_WS_BASE=ws://localhost:8080
