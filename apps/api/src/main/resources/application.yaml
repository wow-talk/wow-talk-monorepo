spring:
  profiles:
    default: local
  application:
    name: wow-talk

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

wowtalk:
  cors:
    allowed-origins: ${WOWTALK_CORS_ALLOWED_ORIGINS:http://localhost:3000}
