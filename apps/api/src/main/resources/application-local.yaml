spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wowtalk
    username: wowtalk
    password: wowtalk
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
